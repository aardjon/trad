///
/// Static information about the application itself. This data does not change at runtime.
///
library;

/// The official name of the mobile application, as being displayed to the user.
const String applicationName = 'trad';

/// The current version number of the mobile application.
/// If not on a tag (i.e. "development mode"), this is usually the version number that was included
/// in the last tag.
/// When incrementing this version number, please also change the 'version:' key in the global
/// pubspec.yaml file (both values must always be equal)!
const String applicationVersion = '0.1.0';

/// The label of the current major version, as displayed to the user.
const String majorVersionName = '[NONE]';
