name: core
description: The 'core' ring of the trad mobile application.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.4.0

# Add regular dependencies here.
dependencies:
  crosscuttings:
    path: ../crosscuttings
  meta: ^1.12.0

dev_dependencies:
  dlcov: ^4.2.1
  mocktail: ^1.0.0
  test: ^1.24.0
  very_good_analysis: ^6.0.0
